# Web runtime — and *only* the web runtime.
#
# The hosted Django function installs exactly this file, so every line here is
# weight in a bundle capped at 500 MB. The ETL dependencies (pandas, pyarrow,
# boto3) are an order of magnitude larger than these three and the site never
# imports one of them, so they live in requirements-etl.txt instead.
#
# Local development and CI want both:  pip install -r requirements-etl.txt

Django==5.1.4
psycopg2-binary==2.9.10
python-dotenv==1.0.1
