# Everything the pipeline needs, on top of the web runtime.
#
# This is the file to install for local development, CI, and the GitHub Actions
# refresh jobs — it is a superset. requirements.txt stays minimal because the
# hosted web function installs that one on its own.

-r requirements.txt

# --- Core ETL ---
requests==2.32.3
pandas==2.2.3
pyarrow==18.1.0
boto3==1.35.92

# --- Warehouse ---
SQLAlchemy==2.0.36

# --- Testing ---
pytest==8.3.4
