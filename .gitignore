# Secrets & environment
.env
.env.*
!.env.example

# Virtual environment
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# Logs
logs/*.log
logs/*.log.*

# Local data scratch
*.parquet
*.tmp

# Django
db.sqlite3
django_app/**/migrations/0*.py
# accounts is the one Django app with real, managed tables (everything else
# is managed = False against the read-only warehouse) -- its migrations are
# the DDL for those tables and must ship with the code that depends on them.
!django_app/accounts/migrations/0*.py
staticfiles/

# graphify stuff
graphify-out/

# OS / editor
.DS_Store
.idea/
.vscode/

# Others
.claude/

# Learning log (kept locally, not tracked)
for_learning.md
# Vercel
.vercel

# Task log (kept locally, not tracked)
tasks.md
